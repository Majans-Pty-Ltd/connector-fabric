# connector-fabric — Project Instructions## What Is This?Python MCP server + HTTP API providing access to Microsoft Fabric semantic models — DAX queries, schema lookups, workspace discovery, pipeline operations, and dataset refresh.**Two deployment modes**:- **Local (stdio)**: `mcp_server.py` — full-featured, Windows-only XMLA + TOM + REST tools- **Remote (Container App)**: `http_server.py` — XMLA (if Windows) + REST API + StreamableHTTP MCP**DAX execution**: on the remote server, `fabric_dax_query` tries XMLA first (bypasses PBI REST permission chain issues with composite/DirectQuery models), falls back to REST API automatically; explicit `fabric_dax_query_xmla` and `fabric_dax_query_rest` tools also available there. The stdio server's `fabric_dax_query` is XMLA-only.## Architecture```AGENTS (Container Apps, internal)           TEAM MEMBERS (Windows, external)────────────────────────────────            ────────────────────────────────agent-hr, agent-costing...            Claude Code        │                                         │ stdio        │ POST /call-tool                         ▼        │ + X-API-Key header               start-mcp.cmd        │ (SP token, full access)           → get-user-token.py (MSAL device code, cached)        ▼                                   → npx mcp-remote https://fabric.majans.com/mcp┌─────────────────────────────────────────────────────────────────┐│  connector-fabric (Azure Container App, external ingress)       ││  https://fabric.majans.com                                      ││                                                                 ││  /mcp        → FastMCP StreamableHTTP (MCP protocol)            ││                Bearer token from user → PBI API as that user    ││                Fabric enforces workspace roles natively          ││                                                                 ││  /call-tool  → Legacy REST (backward compat for agents)         ││                X-API-Key required → SP token → PBI API          ││                                                                 ││  /health     → Container App probe                              │└────────────────────────────┬────────────────────────────────────┘                             │                             ▼                  Power BI REST API (executeQueries)                  Enforces workspace role per token identity```## Tech Stack- Python 3.12 with `mcp` (FastMCP) — MCP server + StreamableHTTP- `pyadomd` + `pythonnet` (CLR) — ADOMD.NET bridge for XMLA (Windows-only, `mcp_server.py`)- `FastAPI` + `uvicorn` — HTTP server (`http_server.py`)- `requests` — Fabric REST API calls- Auth: Service Principal (agents) + User tokens via MSAL device code (team members)## File Structure```http_server.py          # HTTP server: /call-tool (REST) + /mcp (StreamableHTTP) + /healthauth.py                 # Token context var + ASGI middleware for Bearer extractionjwt_validator.py        # Managed-identity JWT validation (audience + MCP.Invoke role)http_client.py          # Async HTTP client — per-tier timeouts, retries, circuit breakerlro.py                  # Long-running job registry (refresh / pipeline tracking)mcp_server.py           # Local MCP server — all tools via @mcp.tool() (stdio, Windows)get-user-token.py       # MSAL device-code flow for user auth (client script)start-mcp.cmd           # Wrapper: get token → launch mcp-remote as stdio proxyschemas/                # Cached table/column/measure JSON per datasetadomd_package/  lib/net45/            # Bundled ADOMD.NET DLL (Windows-only)Dockerfile              # Container image (http_server, auth, jwt_validator, http_client, lro + schemas)requirements-http.txt   # Server dependencies (FastAPI, MCP, requests)requirements-client.txt # Client dependencies (msal)requirements.txt        # Full local dependencies.env.template           # 1Password op:// referencesworkflows.yaml          # Email workflows owned by this repo (see ~/memory/framework_email_workflow_registry.md for master registry).github/workflows/  ci.yml                # Lint (ruff)  deploy.yml            # 1Password → ACR build → Container App update  email-workflow-check.yml  # PR gate: email-sending code changes must update workflows.yaml```## Auth Model### Agent calls (SP token, full access)- `POST /call-tool` with `X-API-Key` header- Server uses Service Principal client credentials for PBI API- API key stored in 1Password: `Fabric MCP API Key`### User calls (per-user Fabric permissions)- `/mcp` StreamableHTTP with `Authorization: Bearer <user_token>`- ASGI middleware extracts token → stored in `contextvars.ContextVar`- Tool functions call PBI API with user's token- Fabric enforces workspace roles: user with Viewer on DEMAND can query SCANv2, gets 403 on FINANCIALv2- Entra app: `Fabric-MCP-User` (`cf4685ef-d594-4ede-961d-5c3554be3974`), public client, delegated `Dataset.Read.All` + `Workspace.Read.All` + `Dataflow.ReadWrite.All` (the last needed for dataflow introspection — Fabric `getDefinition` requires write-tier scope even to read the M-query; connector only calls read endpoints)## Configured Workspaces & Datasets7 Fabric workspaces, 21 datasets (the `WORKSPACES` registry in `mcp_server.py` / `http_server.py` is the source of truth):| Workspace | Datasets ||-----------|----------|| PRODUCT | CONSUMERv2, MAGIC || DEMAND | SALESv2, SCANv2, STORE, SCAN TOTAL GROCERY || SUPPLY | AM, CUSTOMER SERVICE v2, INVENTORYV2, MACHINE, MANUFACTURING V3, MCPHEE_COST, PURCHASINGV3 || REVIEW | FINANCIALv2, PLANAUDIT, THREE-WAY, PRODUCTIONCOST, COSTINGv2 || FIELD | FIELD || HR | HR || Majans Fabric | MajansLakehouse |Default dataset: **SCANv2**## HTTP Server Tools (http_server.py)| Tool | Description ||------|-------------|| `fabric_dax_query` | Execute DAX query (EVALUATE syntax) — XMLA first, REST fallback || `fabric_dax_query_xmla` / `fabric_dax_query_rest` | Single-transport DAX variants (XMLA-only / REST-only) || `fabric_list_tables` | List tables/columns from cached schema || `fabric_get_schema` | Get cached schema for a dataset || `fabric_list_datasets` | List all datasets grouped by workspace || `fabric_discover_workspaces` | Discover accessible workspaces (per-user when Bearer token provided) || `fabric_list_workspace_items` | List items in a workspace (semantic models, pipelines, dataflows...) || `fabric_get_refresh_history` / `fabric_trigger_refresh` | Dataset refresh history / fire-and-forget trigger || `fabric_get_pipeline_runs` / `fabric_trigger_pipeline` | Pipeline runs / fire-and-forget trigger || `fabric_list_dataflows` / `fabric_get_dataflow_transactions` / `fabric_get_dataflow_definition` | Dataflow reads || `fabric_refresh_dataset` / `fabric_run_pipeline` | LRO versions of refresh/pipeline — return `job_id`, server polls to completion || `fabric_check_job` / `fabric_list_jobs` / `fabric_cancel_job` | LRO job status / list / cancel |## Local MCP Tools (mcp_server.py)Additional tools available in stdio mode (Windows-only XMLA/TOM; the workspace-item, refresh, pipeline, and dataflow-read tools above are available here too):- `fabric_list_measures` — list visible measures (live XMLA)- `fabric_test_xmla` — test XMLA connectivity- `fabric_refresh_schema` — live-query XMLA and update cached schema- `fabric_get_dataflow_definition` / `fabric_update_dataflow_definition` — read / **WRITE** a Gen2 dataflow's M-query definition (Items API `getDefinition`/`updateDefinition`; write added 2026-06-08 — mirrors read, SP token already carries `Dataflow.ReadWrite.All`, LRO-polled; overwrites the full `mashup.pq`, does NOT auto-refresh)- `fabric_alter_measure` / `fabric_create_measure` / `fabric_delete_measure` — TOM measure authoring on published models (in-place, metadata-only, no refresh needed)- `fabric_create_calc_table` / `fabric_update_table` — TOM: create calculated table from M / update table properties- `fabric_alter_partition_m` — modify a table partition's M expression in-place (preserves TableID, relationships, table properties — unlike drop-and-recreate)- `fabric_create_relationship` / `fabric_alter_relationship` / `fabric_delete_relationship` — TOM relationship authoring## Commands```bash# Local MCP server (Claude Code stdio)python mcp_server.py# HTTP server (local testing)op run --env-file=.env.template -- python http_server.py# Refresh static schemas (Windows-only, XMLA)python scripts/refresh_schemas.pypython scripts/refresh_schemas.py SCANv2 FINANCIALv2# User token (first-time device code auth)pip install -r requirements-client.txtpython get-user-token.py```## Team Member Onboarding (5 min)1. `pip install msal`2. `python get-user-token.py` → follow device code prompt (browser login)3. Add to `~/.claude/.mcp.json`:   ```json   "fabric": {     "type": "stdio",     "command": "cmd",     "args": ["/c", "C:\\...\\connector-fabric\\start-mcp.cmd"]   }   ```4. Restart Claude Code → `fabric_dax_query`, `fabric_list_datasets` etc. available## Deployment- **Container App**: `connector-fabric` in `rg-majans-agents` (Australia East)- **Ingress**: External, port 8010, min 1 / max 3 replicas- **Custom domain**: `fabric.majans.com` (managed SSL cert)- **CI/CD**: Push to main → `deploy.yml` → 1Password secrets → ACR build → Container App update- **GitHub secrets**: `OP_SERVICE_ACCOUNT_TOKEN`, `AZURE_CREDENTIALS` (repo-level)## Registering with Claude Code**Local stdio (Windows, full tools):**```json{  "command": "python",  "args": ["C:\\...\\connector-fabric\\mcp_server.py"]}```**Remote via start-mcp.cmd (per-user permissions):**```json{  "type": "stdio",  "command": "cmd",  "args": ["/c", "C:\\...\\connector-fabric\\start-mcp.cmd"]}```## Key Notes- MCP SDK requires `transport_security` with `allowed_hosts` for custom domains (DNS rebinding protection)- `pythonnet` must be installed for XMLA tools — not in `requirements.txt`- ADOMD.NET DLL is bundled locally (`adomd_package/lib/net45/`) — no system install needed- XMLA tools are Windows-only, REST tools work cross-platform- Token cache for user auth: `~/.connector-fabric/token_cache.bin`