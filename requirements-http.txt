fastapi>=0.115.0
uvicorn>=0.32.0
# httpx replaces `requests` for async, HTTP/2-capable, properly-configured
# timeouts and connection pooling. `requests` is kept (transitive via msal
# etc.) but new code should use http_client.request().
httpx>=0.27.0
requests>=2.32.0
python-dotenv>=1.0.0
pydantic>=2.0.0
mcp>=1.26.0
PyJWT>=2.8.0
cryptography>=42.0.0
cachetools>=5.3.0
