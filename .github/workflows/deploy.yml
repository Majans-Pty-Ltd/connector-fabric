name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Load secrets from 1Password
        uses: 1password/load-secrets-action@v2
        with:
          export-env: true
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
          AZURE_TENANT_ID: op://Majans Dev/Majans Service Principal/tenant_id
          AZURE_CLIENT_ID: op://Majans Dev/Majans Service Principal/client_id
          AZURE_CLIENT_SECRET: op://Majans Dev/Majans Service Principal/client_secret
          FABRIC_API_KEY: op://Majans Dev/Fabric MCP API Key/credential

      - uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Build & push to ACR
        run: |
          az acr build --registry acrmajansagents \
            --image connector-fabric:${{ github.sha }} .

      - name: Deploy to Container Apps
        run: |
          az containerapp update \
            --name connector-fabric \
            --resource-group rg-majans-agents \
            --image acrmajansagents.azurecr.io/connector-fabric:${{ github.sha }} \
            --set-env-vars \
              AZURE_TENANT_ID="$AZURE_TENANT_ID" \
              AZURE_CLIENT_ID="$AZURE_CLIENT_ID" \
              AZURE_CLIENT_SECRET="$AZURE_CLIENT_SECRET" \
              FABRIC_API_KEY="$FABRIC_API_KEY"

  smoke-test:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Verify revision is ready
        run: |
          for i in $(seq 1 12); do
            LATEST=$(az containerapp revision list \
              --name connector-fabric --resource-group rg-majans-agents \
              --query "sort_by([?properties.active], &properties.createdTime) | [-1].{prov:properties.provisioningState, run:properties.runningState}" \
              -o json 2>/dev/null) || LATEST="{}"
            PROV=$(echo "$LATEST" | jq -r '.prov // "unknown"')
            RUN=$(echo "$LATEST" | jq -r '.run // "unknown"')
            echo "[$(( i * 10 ))s] Provisioning: $PROV, Running: $RUN"
            if [[ "$PROV" == "Provisioned" ]]; then break; fi
            sleep 10
          done
          [[ "$PROV" == "Provisioned" ]] || { echo "::error::Revision not ready"; exit 1; }

      - name: Health check
        run: |
          HTTP_CODE=$(curl -s -o /tmp/h.json -w '%{http_code}' --retry 3 --retry-delay 10 --max-time 30 https://fabric.majans.com/health)
          echo "HTTP: $HTTP_CODE, Body: $(cat /tmp/h.json)"
          [[ "$HTTP_CODE" =~ ^2 ]] || { echo "::error::Health check failed: HTTP $HTTP_CODE"; exit 1; }
